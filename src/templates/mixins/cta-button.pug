mixin cta-button(text='Zadzwoń', type='primary', size='normal')
    a.btn(class=`btn-${size} btn-${type}` href=`tel:+48721587252`)&attributes(attributes)
        if text
            span.btn-text #{text}
        if block
            block
