mixin price(title, price)
    .card.card-white.card-column&attributes(attributes)
        .card__header
            .h6.text-bold= title
        .card__footer
            div od
            .text-accent.h3.text-bold= price
